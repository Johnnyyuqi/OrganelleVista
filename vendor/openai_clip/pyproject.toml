[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
[project]
name = "clip"
version = "1.0"
dependencies = ["ftfy", "packaging", "regex", "tqdm", "torch", "torchvision"]
[tool.setuptools.package-data]
clip = ["*.gz"]

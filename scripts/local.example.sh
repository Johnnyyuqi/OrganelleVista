# Copy this example to a local file and set your own dataset/checkpoint paths.
export PYTHON_BIN="${PYTHON_BIN:-python}"
export DATASET_DIR=/path/to/paired_dataset
export RESUME_FROM=/path/to/full_checkpoint.pt
export GPU_IDS=2,3
